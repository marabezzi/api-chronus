# Compose V2 — campo "version" removido (deprecado)
# https://docs.docker.com/compose/compose-file/
services:
  chronus:
    build: .
    container_name: chronus-api

    ports:
      - "8081:8081"

    env_file:
      - .env

    environment:
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILES_ACTIVE:-dev}

    restart: unless-stopped